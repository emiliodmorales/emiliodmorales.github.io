# Web-Portfolio

## First-time set-up

Open a terminal in the Web Portfolio directory.
Run the command:

```
npm install
```

## How to run

Open a terminal in the Web Portfolio directory.
Run the command:

```
npm run dev
```
